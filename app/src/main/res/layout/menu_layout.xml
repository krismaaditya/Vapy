<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <ImageButton
        android:id="@+id/marketImageButton"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignLeft="@+id/chatImageButton"
        android:layout_alignParentTop="true"
        android:layout_alignStart="@+id/chatImageButton"
        android:layout_marginTop="11dp"
        android:backgroundTint="@android:color/background_light"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/vapy_market_logo" />

    <ImageButton
        android:id="@+id/diskusiImageButton"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignLeft="@+id/accountImageButton"
        android:layout_alignStart="@+id/accountImageButton"
        android:layout_alignTop="@+id/marketImageButton"
        android:backgroundTint="@android:color/background_light"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/vapy_discussion_logo" />

    <ImageButton
        android:id="@+id/chatImageButton"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/marketImageButton"
        android:layout_marginLeft="14dp"
        android:layout_marginStart="14dp"
        android:layout_marginTop="15dp"
        android:backgroundTint="@android:color/background_light"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/vapy_chat_logo" />

    <ImageButton
        android:id="@+id/accountImageButton"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/chatImageButton"
        android:layout_marginEnd="12dp"
        android:layout_marginRight="12dp"
        android:backgroundTint="@android:color/background_light"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/vapy_account_logo" />

    <Button
        android:id="@+id/logoutButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/cmykred_button_bg"
        android:text="LOGOUT"
        android:textColor="@android:color/white"
        android:layout_marginBottom="24dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:id="@+id/exitButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/accountImageButton"
        android:layout_alignRight="@+id/accountImageButton"
        android:layout_alignTop="@+id/logoutButton"
        android:text="EXIT" />

</RelativeLayout>